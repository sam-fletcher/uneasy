// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: laws_rumors.sql

package dbgen

import (
	"context"
)

const createLaw = `-- name: CreateLaw :one


INSERT INTO laws (game_id, text, addendum, origin_plan_id, signatory_id, display_order)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, game_id, text, addendum, origin_plan_id, signatory_id, created_at, is_active, display_order
`

type CreateLawParams struct {
	GameID       int64   `db:"game_id" json:"game_id"`
	Text         string  `db:"text" json:"text"`
	Addendum     *string `db:"addendum" json:"addendum"`
	OriginPlanID *int64  `db:"origin_plan_id" json:"origin_plan_id"`
	SignatoryID  *int64  `db:"signatory_id" json:"signatory_id"`
	DisplayOrder int16   `db:"display_order" json:"display_order"`
}

// sqlc query file for laws and rumors.
// ── Laws ─────────────────────────────────────────────────────────────
func (q *Queries) CreateLaw(ctx context.Context, arg CreateLawParams) (Law, error) {
	row := q.db.QueryRow(ctx, createLaw,
		arg.GameID,
		arg.Text,
		arg.Addendum,
		arg.OriginPlanID,
		arg.SignatoryID,
		arg.DisplayOrder,
	)
	var i Law
	err := row.Scan(
		&i.ID,
		&i.GameID,
		&i.Text,
		&i.Addendum,
		&i.OriginPlanID,
		&i.SignatoryID,
		&i.CreatedAt,
		&i.IsActive,
		&i.DisplayOrder,
	)
	return i, err
}

const createRumor = `-- name: CreateRumor :one

INSERT INTO rumors (game_id, text, target_asset_id, origin_plan_id, source_player_id, display_order)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, game_id, text, target_asset_id, origin_plan_id, source_player_id, is_active, created_at, display_order
`

type CreateRumorParams struct {
	GameID         int64  `db:"game_id" json:"game_id"`
	Text           string `db:"text" json:"text"`
	TargetAssetID  *int64 `db:"target_asset_id" json:"target_asset_id"`
	OriginPlanID   *int64 `db:"origin_plan_id" json:"origin_plan_id"`
	SourcePlayerID *int64 `db:"source_player_id" json:"source_player_id"`
	DisplayOrder   int16  `db:"display_order" json:"display_order"`
}

// ── Rumors ───────────────────────────────────────────────────────────
func (q *Queries) CreateRumor(ctx context.Context, arg CreateRumorParams) (Rumor, error) {
	row := q.db.QueryRow(ctx, createRumor,
		arg.GameID,
		arg.Text,
		arg.TargetAssetID,
		arg.OriginPlanID,
		arg.SourcePlayerID,
		arg.DisplayOrder,
	)
	var i Rumor
	err := row.Scan(
		&i.ID,
		&i.GameID,
		&i.Text,
		&i.TargetAssetID,
		&i.OriginPlanID,
		&i.SourcePlayerID,
		&i.IsActive,
		&i.CreatedAt,
		&i.DisplayOrder,
	)
	return i, err
}

const deactivateLaw = `-- name: DeactivateLaw :exec
UPDATE laws SET is_active = FALSE WHERE id = $1
`

func (q *Queries) DeactivateLaw(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deactivateLaw, id)
	return err
}

const deactivateRumor = `-- name: DeactivateRumor :exec
UPDATE rumors SET is_active = FALSE WHERE id = $1
`

func (q *Queries) DeactivateRumor(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deactivateRumor, id)
	return err
}

const getRumorByID = `-- name: GetRumorByID :one
SELECT id, game_id, text, target_asset_id, origin_plan_id, source_player_id, is_active, created_at, display_order FROM rumors WHERE id = $1
`

func (q *Queries) GetRumorByID(ctx context.Context, id int64) (Rumor, error) {
	row := q.db.QueryRow(ctx, getRumorByID, id)
	var i Rumor
	err := row.Scan(
		&i.ID,
		&i.GameID,
		&i.Text,
		&i.TargetAssetID,
		&i.OriginPlanID,
		&i.SourcePlayerID,
		&i.IsActive,
		&i.CreatedAt,
		&i.DisplayOrder,
	)
	return i, err
}

const listLaws = `-- name: ListLaws :many
SELECT id, game_id, text, addendum, origin_plan_id, signatory_id, created_at, is_active, display_order FROM laws WHERE game_id = $1 AND is_active = TRUE
ORDER BY display_order ASC, created_at ASC
`

func (q *Queries) ListLaws(ctx context.Context, gameID int64) ([]Law, error) {
	rows, err := q.db.Query(ctx, listLaws, gameID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Law{}
	for rows.Next() {
		var i Law
		if err := rows.Scan(
			&i.ID,
			&i.GameID,
			&i.Text,
			&i.Addendum,
			&i.OriginPlanID,
			&i.SignatoryID,
			&i.CreatedAt,
			&i.IsActive,
			&i.DisplayOrder,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRumors = `-- name: ListRumors :many
SELECT id, game_id, text, target_asset_id, origin_plan_id, source_player_id, is_active, created_at, display_order FROM rumors WHERE game_id = $1 AND is_active = TRUE
ORDER BY display_order ASC, created_at ASC
`

func (q *Queries) ListRumors(ctx context.Context, gameID int64) ([]Rumor, error) {
	rows, err := q.db.Query(ctx, listRumors, gameID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Rumor{}
	for rows.Next() {
		var i Rumor
		if err := rows.Scan(
			&i.ID,
			&i.GameID,
			&i.Text,
			&i.TargetAssetID,
			&i.OriginPlanID,
			&i.SourcePlayerID,
			&i.IsActive,
			&i.CreatedAt,
			&i.DisplayOrder,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setRumorSourceHidden = `-- name: SetRumorSourceHidden :exec
UPDATE rumors SET source_player_id = NULL WHERE id = $1
`

// Remove the source attribution from a rumor (Spread Rumors hide-source option).
func (q *Queries) SetRumorSourceHidden(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, setRumorSourceHidden, id)
	return err
}
